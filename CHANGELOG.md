# 更新日志

版本号与作者内部开发仓同步发布，因此历史版本可能不连续；本列表为摘要（每项仅保留标题），从 v0.5.9（开源起点）起记录。版本号唯一来源：根 package.json 的 version。

## [0.12.1] — 2026-09-25

### 变更
- **内核升级 0.12.66 → 0.12.67（@thincoder/core 0.9.4 → 0.9.5），WebUI 与全局 TUI 同步升级**

## [0.12.0] — 2026-09-25

### 新增
- **设置页「模型」字段：有清单就选项、没清单才文本**（用户需求，起因＝「各家供应商模型名是否都区分大小写」的实测追问）

### 变更
- **清单拉取失败的文案改为「一句人话」**（用户裁定）

### 修复
- **删渠道后子代理模型引用悬挂**（用户实测提出）
- **候选面板按「当前值」预筛选，打开下拉几乎看不到别的模型**（本批自造缺陷，e2e 当场抓住）
- **设置页底部出现一条横向滚动条**（用户实测提出）
- **去掉三处冗余 tooltip**（用户裁定）

## [0.10.0] — 2026-09-25

### 新增
- **设置页「子代理模型」区块：按类型为子任务指定模型**（用户需求）
- **供应商行「模型」下拉：同一供应商的任意模型设为主线**
- **子代理类型名中文单源**
- **子代理报告进时间线（读得到原文）**

### 变更
- **子代理面板改为「本轮视图」**（用户裁定）

### 修复
- **面板标题「失败 N」误报**（用户实测：显示「5/5 失败 1」而并无失败子代理）
- **子代理报告路径曾造成「已派发」计数虚高 + 重启后 `role#id` 撞车覆盖旧记录**（用户实测发现 48/48、失败 7 与所见行对不上）
- **运行中的子代理/工具行里 spinner 显示成「一条竖线」**（用户实测）
- **异步普通子代理失败在面板显示假绿「已完成」**（用户实测，故意配了配额耗尽的模型派 5 个子代理）

## [0.9.0] — 2026-09-24

### 新增
- **挂起会话驱动：后台子代理的报告会自己进会话了**（用户实测提出）

### 变更
- **新事件 `suspension` + `run_start`/`run_end` 的 `digest` 标记 + 快照 `active[].suspended`**
- **长任务完成音改为真·全局响**（用户裁定）
- **子代理面板数字改「已结束/已派发」进度式**
- **顶栏「挂起中」胶囊删除**
- **收尾分隔行（时间/用量那行）去掉 Tooltip**

## [0.8.6] — 2026-09-24

### 修复
- **Windows 上选目录 / 选文件切不到别的盘**（用户实测）

## [0.8.5] — 2026-09-24

### 修复
- **运行中每条助手发言下方都在闪竖线**（用户实测）
- **切走视图再回来，输入框自己冒出以前发过的文字**（用户实测）
- **连续两张审批/提问弹窗之间输入状态串味**

## [0.8.4] — 2026-09-24

### 修复
- **运行收尾的 MCP 冲刷会带着 undefined 的实例条目抛错**（0.8.3 引入）

## [0.8.3] — 2026-09-24

### 修复
- **MCP opt-in 盲区：新增 server 对旧格式项目自动勾选**（0.4.3 遗留）

### 变更
- **新装 server 默认勾选「安装发起项目」**
- **公开仓 README 增补三块内容**

## [0.8.1] — 2026-09-24

### 新增
- **顶栏思考程度选择器（内核 `/think` 等价面）**
- **输入框「＋」选文件插路径**
- **文档预览面板支持代码类文件**

### 变更
- **顶栏审批模式改为胶囊 + 菜单**（与 provider/model、think 胶囊同款交互）
- **全站悬停提示统一为自定义 tooltip**
- **左侧导航「任务」更名「定时」**（消除与顶栏「任务」的歧义，用户拍板）

### 修复
- **滚动条交角白块**
- **历史面板 tooltip 串扰**
- 审批策略悬浮提示去掉「

## [0.8.0] — 2026-09-23

### 新增
- **一键半自动更新**（关于页检测到新版本后可一键完成更新：拉取公开仓 → 安装依赖 → 重新构建并自动换入，全程日志实时可见，完成后重启服务生效）

## [0.7.1] — 2026-09-23

### 修复
- **全新克隆按快速开始构建失败（`vite: command not found`）**

### 变更
- **README 快速开始补 `git clone` / `cd` 前置两步、环境要求修正为 Node ≥ 22.5**（与 `package.json` engines 对齐）

## [0.7.0] — 2026-09-23

### 修复
- **Windows 兼容两处阻断**（静态审计发现）
- **Windows 兼容四处适配**（同批审计发现）

### 新增
- **「关于」页改版为单卡**
- **「关于」页 WebUI 自身最新版检查**（口径同内核检查）

## [0.6.0] — 2026-09-23

### 变更
- **「关于」自设置页迁出为左侧导航独立入口**

## [0.6.1] — 2026-09-23

### 变更
- **公开版更新日志口径修正**
